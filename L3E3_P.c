#include <stdio.h>
#include <stdlib.h>

char graf(int altBar); // (*) -> linhas
int val_Ale(int n);

int main()
{
    float vect[];                              // numeros reais
    int k;                                     // numero sub-intervalos de [0,1]
    int n;                                     // numero de valores em [0,1]
    int largInt = (max(vect) - min(vect)) / k; // largura intervalos
}